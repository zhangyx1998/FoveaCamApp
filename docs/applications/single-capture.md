# Single Capture

**Seed confidence: LOW — planner has not worked this app; auditor derives
everything from code (modules/single-capture/).**

## Purpose (hypothesis)
One-shot synchronized capture of the camera set to disk (possibly hardware-
triggered L/R — note CAM0/center has no trigger cable, see project memory),
with save-path controls and capture metadata.

## Pipeline / UI / Expected behavior
(auditor derives)

## Known/suspected issues
- If it captures via triggers: only L/R are hardware-triggerable on this rig.

## Open questions (for the user)
(auditor fills)
