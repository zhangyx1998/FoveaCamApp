{
  "name": "bench-recorder",
  "private": true,
  "type": "module",
  "description": "Stage 5 / B-4: MCAP container format evaluation bench for the recorder rewrite. Not part of the npm workspace build — run directly with /opt/homebrew/bin/node.",
  "scripts": {
    "bench": "node src/bench.ts",
    "verify-seek": "node src/verify-seek.ts",
    "crash-recovery": "node src/crash-recovery.ts"
  },
  "devDependencies": {
    "@mcap/core": "2.2.1",
    "@mcap/nodejs": "1.1.0",
    "lz4-napi": "2.9.0",
    "zstd-napi": "0.0.12"
  }
}
